syntax = "proto2";

package press.models;

option go_package = 'press/models';

message Route {
    optional string uuid = 1;
    optional string path = 2;
    oneof target {
        string file = 10;
        string page_uuid = 11;
    }
}
